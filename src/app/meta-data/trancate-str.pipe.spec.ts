import { TrancateStrPipe } from './trancate-str.pipe';

describe('TrancateStrPipe', () => {
  it('create an instance', () => {
    const pipe = new TrancateStrPipe();
    expect(pipe).toBeTruthy();
  });
});
